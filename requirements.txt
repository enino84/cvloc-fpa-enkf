# pyteda pulls numpy, scipy, matplotlib, scikit-learn, pandas, xarray,
# netCDF4 and pyshtools by itself. Versions are pinned so that a rebuild
# months from now reproduces the same numbers.
pyteda==0.5.0
numpy>=1.26,<3
scipy>=1.11
pandas>=2.0
matplotlib>=3.7
scikit-learn>=1.3
pytest>=7.4
jupyter>=1.0
